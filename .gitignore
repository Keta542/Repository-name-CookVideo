node_modules/
dist/
*.log
.DS_Store

# Implementation briefs prepared for Claude executions -- working artifacts, not
# source-controlled documents. .cookvideo/EXECUTION_LOG.json IS tracked (it's the
# durable execution history), only the briefs themselves are ignored.
.cookvideo/briefs/
